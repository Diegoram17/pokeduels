// Tailwind CSS v4 is processed by the @tailwindcss/vite plugin (see vite.config.ts),
// which does not require a PostCSS config file. This file exists to satisfy the
// project scaffolding spec; it is intentionally empty and inert.
export default {
  plugins: {},
}