# Frontend runtime configuration. Copy this file to `.env` and adjust the
# URLs to point at your running backend (server.js defaults to port 3000).
VITE_API_URL=http://localhost:3000
VITE_WS_URL=http://localhost:3000