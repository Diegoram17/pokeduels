{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate:up": "node --env-file=.env ./node_modules/node-pg-migrate/bin/node-pg-migrate.js up",
    "migrate:down": "node --env-file=.env ./node_modules/node-pg-migrate/bin/node-pg-migrate.js down",
    "migrate:create": "node-pg-migrate create",
    "preseed": "node ../scripts/sync-seed-data.mjs",
    "seed": "node --env-file=.env seed/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pg": "^8",
    "node-pg-migrate": "^7"
  },
  "devDependencies": {
    "vitest": "^4"
  }
}