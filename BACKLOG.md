# Backlog: Pokémon Duels

> Estado del repo: sistema completo implementado y mergeado a `master` (Fases 1–7 de producto + Fases 1–6 de diagnóstico, ver ítems abajo). Dos paquetes npm independientes: `backend/` (Express + Socket.IO + Postgres/Neon, plain JS ESM) y `frontend/` (Vite + React + TypeScript). El motor de combate corre server-side en `backend/engine/`; el cliente lo consume vía REST + WebSocket.
>
> Nota: `poke-duel-engine/` (motor copiado de otro proyecto) y `frontend/src/engine/` (vestigial) se eliminaron en la Fase 5 (BACKLOG #16). `MoveIndex` vive en `frontend/src/lib/moveIndex.ts`.

| # | Item | Alcance | Depende de | Contexto extra requerido | Estado |
|---|---|---|---|---|---|
| 1 | Esquema de BD, migraciones y seed | Tablas de ADR-0002 (`players`, `types`, `pokemons`, `type_effectiveness`, `rooms`, `room_players`, `team_selections`, `duels`, `duel_pokemon_state`, `moves`) + seed del catálogo (ya curado en `seed-data.json`) y de la matriz de tipos 18×18 | — | Matriz 18×18 (324 filas) + 54 pokémon seedeados. | ✅ Implementado, seedeado en Neon real y archivado (✅ Mergeado a `master`). Follow-up `db-schema-seed-migration-fix` (constraint `chk_move_index_required_for_attack`) también archivado. Sin pendientes |
| 2 | API REST: sesión, catálogo, salas | `POST /api/session`, `GET /api/pokemons`, `GET /api/type-effectiveness`, `GET/POST /api/rooms`, `POST /api/rooms/:code/join` (RF-1, RF-2, ADR-0003) | #1 | — | ✅ Implementado y archivado (`sdd/rest-api-session-catalog-rooms`, 2026-08-23) — 6 endpoints, 47/47 tests, PASS WITH WARNINGS. Commits `2d91bff`, `045643e` |
| 3 | Motor de combate server-side | Portar `calcularDaño`/`resolverRonda`/máquina de estados (ya validado con TDD en `frontend/src/engine/`) a un servicio Node con acceso a BD (RF-4, TECH-DESIGN §4) | #1 | — | ✅ Implementado y archivado (`sdd/server-duel-engine`, 2026-08-23) — 86/86 tests (74 unit + 12 integración), PASS WITH WARNINGS. Commits `4177ec9`, `55e9781`, `b91b771`. Conectado a endpoints en vivo por #5 (2026-08-23) |
| 4 | Sala en tiempo real (WebSocket) | `room:join/ready/leave`, `team:select_starter/roster`, exclusividad de starter con constraint de BD, reconexión con timeout 60s (RF-2.5–2.7, RF-3.2, ADR-0004, ADR-0005) | #1, #2 | — | ✅ Implementado y archivado (`sdd/realtime-room-websocket`, 2026-08-23) — 49/49 tests, PASS WITH WARNINGS. 16 commits (3 PRs apilados) en `db-schema-seed/pr3`, incl. fix post-verify `0a7c97d`. Conectado a endpoints en vivo por #5 (2026-08-23) |
| 5 | Ciclo de duelo en tiempo real | `duel:select_lead`, `duel:switch_decision`, `duel:select_action`, timeout 10s, `duel:turn_resolved` (RF-4.7–4.12, RF-6.1, RF-6.3, TECH-DESIGN §4.3–4.5) | #3, #4 | — | ✅ Implementado y archivado (`sdd/realtime-duel-cycle`, 2026-08-23) — 21/21 tasks, PASS (11/11 escenarios tras 1 fix post-verify de cobertura). 3 PRs apilados `realtime-duel-cycle/pr1→pr3` sobre `db-schema-seed/pr3`, ✅ Mergeado a `master`. Incluye bootstrap de duelo (crea `duels`/`duel_pokemon_state` al quedar ambos jugadores listos), lock `SELECT...FOR UPDATE` contra race de bootstrap concurrente |
| 6 | Fin de duelo: KO, rendición, desconexión | RF-5.1, RF-5.5 (rendición con confirmación), RF-6.2 (desconexión mid-duelo) | #5 | — | ✅ Implementado y archivado (`sdd/duel-end-ko-surrender-disconnect`, 2026-08-23) — 18/18 tasks, PASS WITH WARNINGS (0 críticos). RF-5.1 resultó ya implementado por el ítem #5; alcance real fue RF-5.5 + RF-6.2. 3 PRs apiladas `duel-end-ko-surrender-disconnect/pr1→pr3` (commits `4fa342d`, `2621bf6`, `a88f716`, + fix post-verify `aa65a95`) sobre `frontend-rest-ws/pr3`, ✅ Mergeado a `master`. Requirió mergear `realtime-duel-cycle/pr3` (ítem #5) a `frontend-rest-ws/pr3` primero (commit `da02d92`) — habían divergido de la misma base sin mergearse entre sí |
| 7 | Torneo de 4: bracket y ranking final | RF-5.2–5.4, sin arrastre de HP/PP entre duelos del bracket (ADR-0007) | #6 | — | ✅ Implementado y archivado (`sdd/tournament-bracket-ranking`, 2026-08-24) — 25/25 tasks, PASS WITH WARNINGS (0 críticos). 3 PRs apilados `tournament-bracket-ranking/pr1→pr3` sobre `duel-end-ko-surrender-disconnect/pr3` (commits `5080dd4`, `d121fe7`, `2b84dd4`, `3766a44`, `8be12e3`, `9fd36b3`, `e1e9437`), ✅ Mergeado a `master`. Backend: 260 tests pass / 0 failed / 153 skipped (DB-gated, no DATABASE_URL en sandbox; recomendación: re-ejecutar suite con Postgres para cerrar integración-concurrencia + ADR-0007 proofs). **DB-gated suite re-executed twice independently (`sdd/backend-db-integration-verify`, 2026-08-24), 413/413 passed, all 3 named proofs witnessed — closes integration-concurrency + ADR-0007 gap.** |
| 8 | Resiliencia operativa | `/health` (ADR-0006) + reconciliación de arranque de duelos/salas huérfanas, `room:aborted` (ADR-0008) | #7 | — | ✅ Implementado y archivado (`sdd/operational-resilience`, 2026-08-24) — 46 archivos test / 425 tests PASS. Commits 8e4d02f, 7455d3d, 7e2429d, 58723b1 (corrective test-only). ✅ Mergeado a `master`. |
| 9 | Frontend: integración REST/WS real (sesión, catálogo, salas, draft) | Reemplaza el mock en `LoginScreen`, `LobbyScreen`, `TeamSelectScreen` por el cliente REST/WS real | #2, #4 | — | ✅ Implementado y archivado (`sdd/frontend-rest-ws-integration`, 2026-08-23) — 26/26 tasks + 1 fix post-verify, PASS (16/16 escenarios). 3 PRs apilados `frontend-rest-ws/pr1→pr3` sobre `db-schema-seed/pr3`, ✅ Mergeado a `master`. Identidad de Pokémon migrada a id numérico (nombre visible en UI vía lookup) |
| 10 | Frontend: integración real de duelo y torneo | Reemplaza el mock en `WaitRoomScreen`, `DuelBoardScreen`, `SwapScreen`, `RankingScreen` por el cliente WS real | #7, #9 | Nota de UX pedida por el usuario (2026-08-24, surgida durante el proposal de #7): cuando un jugador termina su duelo del bracket antes que el otro par siga jugando, mostrarle dos botones — "esperar el duelo que falta" vs "ir a tu ranking ya". Si espera, ve el ranking real cuando se resuelve. Si no espera, ve un ranking provisorio con las posiciones aún no decididas en orden aleatorio/placeholder. Confirmado como decisión pura de frontend — el backend de #7 no expone ni necesita ningún estado nuevo para esto, solo emite `room:final_ranking` cuando la sala cierra de verdad. | ✅ Implementado y archivado (`sdd/frontend-duel-tournament-integration`, 2026-08-24) — 19/19 tasks (Fase 1-3 apply + Fase 4 post-verify), PASS tras re-verify (0 críticos, 0 warnings bloqueantes). 225/225 tests, 27 archivos, 0 errores tsc. El primer verify encontró 1 CRITICAL (spec de reconexión mid-duelo — re-emitir `duel:join` al remontar — nunca se tradujo a task); se agregó como Fase 4, se implementó y re-verify confirmó el fix con tests reales. Commits: 3×PR1 (foundation) + `073dda2`, `b75b75c`, `c98c175`, `7ae2a26` (PR2 screens) + `49c5dfb` (PR3 cleanup, borra mock engine) + `046919b` (PR4 post-verify: resync mid-duelo + countdown de líder + dedupe rematch panel). ✅ Mergeado a `master` |
| 11 | Diagnóstico full (arquitectura + UI/UX + adversarial) + verificación punto por punto | 27 hallazgos (7 críticos, 10 altos, 7 medios, 3 bajos) sobre el sistema ya implementado (#1-#10), verificados contra el código real. 5 correcciones de alcance/ubicación sobre el diagnóstico original (la más importante: F1 es explotable en cualquier fase del duelo, no solo al elegir lead, y el fix real va en `switchValidation.js`, no en `duelRepository.js`). (corrección 2026-08-31: `poke-duel-engine/` fue eliminado en Fase 5, BACKLOG #16) | — | Engram: obs #277 (resumen+plan 5 fases), #278 (arquitectura), #279 (UI/UX), #280 (adversarial), #281 (verificación con evidencia file:line) | ✅ Cerrado — diagnóstico y verificación completos (2026-08-24). Desglosado en ítems #12-17 |
| 12 | Fase 1: Seguridad y estabilidad | F1 corregido (guard de fase + `already_active` check en `switchValidation.js`), F2 (rate limiting en eventos WS, desconecta el socket al exceder), F3 (frontend escucha `room:aborted` con banner + botón manual), F4 (CI corre los tests no-gateados sin depender de secrets de Neon — corrección: hoy no corre NINGUNO de los 428 tests del backend, no solo los 162 gateados por DB), F6 (limpiar `duelPhaseStore` al finalizar duelo), F5 (ADR de risk-acceptance para el token OIDC de Vercel, ya gitignored) | #11 | Ver obs #281 (verificación), #283 (proposal), #285 (spec), #286 (design), #288 (tasks), #289 (apply), #293 (verify), #294 (archive) | ✅ Implementado y archivado (`sdd/diagnostic-phase1-security-stability`, 2026-08-25) — 19/19 tasks, PASS WITH WARNINGS (0 críticos). 4 PRs mergeados a `master` (`#1` F4+F6, `#2` F1, `#3` F2, `#4` F3+F5), commits `d229543`/`33f30dd`/`4eaf99e`/`7374039`. 275 tests backend + 234 frontend, 0 fallos. (corrección 2026-08-31: secrets de Neon configurados desde 2026-08-25, ver ítem #18; el test WS de F1 corre en CI real con Neon efímero) |
| 18 | Bug pre-existente: estado AWAITING_LEAD post-bootstrap (a investigar) | Durante el verify de la Fase 1 (obs #293/#294), una corrida acotada contra la Neon real (no aislada, no CI) mostró fallar `duelHandlers.ws.test.js` — "registers the WS-layer round sub-state as AWAITING_LEAD after bootstrap". Confirmado por git log que ninguno de los 4 PRs de la Fase 1 tocó ese código. Señal inconclusa (la corrida se cortó a propósito para no seguir mutando la BD compartida) — falta reproducir en un run de CI aislado (rama efímera de Neon) con los secrets configurados | — | Ver obs #293, #294 | ✅ Cerrado — no reproduce (2026-08-28). El run de CI `32918591683` en `master` (commit `770641c`) corrió el bloque DB-gated contra una branch efímera de Neon (aislado; secrets `NEON_PROJECT_ID`/`NEON_API_KEY` configurados desde 2026-08-25) y pasó **446/446 tests**, incluido `duelHandlers.ws.test.js` › "registers the WS-layer round sub-state as AWAITING_LEAD after bootstrap" (✓ 4513ms). Análisis estático: no hay bug — `bootstrapDuelIfReady` (`backend/ws/duelBootstrap.js:54-57`) setea `getPhaseStore`→`lead_selection` y `getRoundStateStore`→`AWAITING_LEAD` de forma **síncrona antes** de emitir `duel:start`, y el test espera ese evento, así que en un run aislado la aserción es determinística. La señal de obs #293 fue artefacto de una corrida interrumpida contra la Neon compartida no efímera |
| 13 | Fase 2: Frontend Foundation | UX3 (`lang="es"`), UX4 (focus trap en `SurrenderConfirmModal`), UX5 (loading states en lobby/catálogo). ⚠️ UX1 y UX2 salieron de este ítem el 2026-08-25: RNF-2 del PRD pasó de "responsive móvil+desktop" a desktop-only (decisión de producto), lo que deja sin sentido UX2 (breakpoints responsive) y le saca a UX1 (definir las 80+ clases CSS fantasma) su única justificación ("prerequisito para responsive") — sin bug visible que arreglar, UX1 pasa a ser deuda de arquitectura pura y se movió al ítem #15 | #12 (orden recomendado, sin dependencia técnica dura) | Ver obs #279, #281 | ✅ Implementado (2026-08-28) — UX3/UX4/UX5 con TDD, sin ciclo SDD formal (cambio chico, hecho orgánico directo en `master`). Commits `14826b9` (UX3: `lang="es"` en index.html + test de shell), `e7b04c0` (UX4: focus trap Tab/Escape en el modal de rendirse + test), `6789eec` (UX5: skeletons de carga en lobby/catálogo + tests), `447fa0a` (guard: el locale del documento sobrevive a la navegación cliente-side). 267/267 tests frontend, lint sin errores. Se evaluó agregar Playwright para E2E de accesibilidad y se descartó: los 3 sub-ítems ya están cubiertos con tests jsdom/testing-library; un `playwright` suelto sin config/spec/CI no es un setup de E2E — si se quiere E2E es un ítem propio |
| 14 | Fase 3: Backend Refactor | A1 (consolidar los 6 singletons — no 7 — en un registry `DuelContext`), A2 (dividir `duelRepository.js`, 476 líneas cruzando repository/engine/presentación), F7 (graceful shutdown SIGTERM en `server.js`), A5 (test E2E de flujo WS completo `room:ready`→finish→torneo) | #12 (orden recomendado) | Ver obs #278, #281, #332-#354 | ✅ Implementado y archivado (`sdd/diagnostic-phase3-backend-refactor`, 2026-08-29) — 32/32 tasks, PASS WITH WARNINGS (0 críticos, 3 warnings, 3 sugerencias). ✅ Mergeado a `master` 2026-08-31 (PR #15, commit `ab7a0c1`). **El slice F7 (graceful shutdown) se descartó** — Fase 6 (#17) ya shippeó un handler `SIGTERM`/`SIGINT` + `closePool()` equivalente en `server.js`. Se integraron A2 + A1-3a + A1-3b + A5 como un commit squash; 3 conflictos reconciliados. CI PR #15: 57 archivos / 495 tests verdes en Neon efímero. |
| 15 | Fase 4: Frontend Architecture | A3 (extraer hooks de `MockStateProvider.tsx`), A4 (extraer hooks de pantallas monolíticas como `DuelBoard`, 590 líneas), UX1 (definir o eliminar las 80+ clases CSS fantasma — movido desde #13 el 2026-08-25, ver esa fila), UX6 (reducir duplicación de estilos inline), F8 (unificar índice de movimiento 0/1-based en el límite `MockStateProvider.tsx:280,162`) | #12 (orden recomendado) | Ver obs #278, #279, #281, #339-#350 | ✅ Implementado y archivado (`sdd/diagnostic-phase4-frontend-architecture`, 2026-08-29) — 5 slices (PR1 F8 → PR2 UX1/UX6 → PR3 A3 → PR4a A4 → PR4b A4), tasks completas, PASS WITH WARNINGS (0 críticos, 2 warnings, 3 sugerencias). Cadena de feature-branches locales `diagnostic-phase4-frontend-architecture/pr1→pr4b` (tip `1dc48e4`). 38 archivos / 304 tests / exit 0, `tsc -b` exit 0, `oxlint` exit 0. 29 archivos cambiados (+1215 / -734): 6 componentes compartidos nuevos (Modal, ScreenTopbar, NicknameBadge, HpBar, HudCard, GlowBlob) + 2 hooks (useMockPersistence, useDuelSocket) + helper `lib/moveIndex.ts`, ~74 tokens CSS fantasma borrados, `useBotAutomation.ts` eliminado. W1: apply-progress sin tabla formal de evidencia TDD (los marcadores RED están en tasks obs #345). W2: `.pd-topbar` compartido agrega `position:sticky` que 2 topbars inline previos no tenían (analizado como no-op visual, requiere confirmación en QA manual). ✅ Mergeado a `master` (`0ddca11`, 2026-08-31). Gate visual manual: **realizado por el usuario el 2026-08-31**; encontró 5 hallazgos de UX/UI/funcionalidad (salas fantasma en lobby, modal de reglas sin auto-apertura, "ELIGE TU EQUIPO" recortado, freeze de la pantalla de duelo + roster de 6 sin renderizar, botón REVANCHA sin acción) — atendidos en la ronda `fix/qa-round-2`. |
| 16 | Fase 5: Cleanup | F9 (borrar `poke-duel-engine/`, dead code confirmado — ver nota de #11), F10 (habilitar TS strict mode), UX7-10 (skip-nav, labels en room-code y "cambiar apodo" del lobby — el nickname del Login ya está bien, reduced-motion cubre 8 clases no ~7), A6 (renombrar o borrar `damage.ts` vestigial, 0-based sin type effectiveness real) | #12 (orden recomendado) | Ver obs #278-#281, #356-#366 | ✅ Implementado y archivado (`sdd/diagnostic-phase5-cleanup`, 2026-08-29) — 29/29 tasks, PASS WITH CONCERNS (0 críticos, 0 bloqueantes). Rama `diagnostic-phase5-cleanup`, 4 commits sobre el tip de Fase 4 `1dc48e4` (NO sobre `master`): `1658554` (A: F9 borra `poke-duel-engine/` 24 archivos + A6 mueve `MoveIndex` a `lib/moveIndex.ts` y borra `frontend/src/engine/`), `df73787` (B: UX7-10 — skip-link, landmarks `<main>` en las 7 pantallas, aria-labels en los 2 inputs del lobby, regla blanket WCAG de reduced-motion), `8157cc2` (C: F10 — `"strict": true` en ambos tsconfigs, 0 errores tsc), `a0c041f` (D: fix post-archive del hueco de a11y detectado en el verify — el `<main>` de DuelBoard envolvía solo el HUD, ahora envuelve también los controles `duel-command`). 317/317 tests frontend, `tsc -b` exit 0, `oxlint` exit 0 (9 warnings pre-existentes). Decisiones del usuario respetadas: tag-swap del wrapper existente (no `<main>` nuevo), blanket reduced-motion, `SECURITY-REPORT.md` intacto, 1 PR con commits internos. ✅ Mergeado a `master` (`0ddca11`, 2026-08-31). Las refs viejas a `poke-duel-engine` en `SECURITY-REPORT.md` se limpian en la auditoría de docs del cierre del programa |
| 17 | Fase 6: Hardening operativo y deuda de documentación | Hallazgos de la revisión adversarial sin fase asignada en el plan original: sanitización de nickname en servidor (hoy solo se valida en frontend, bypasseable), índices de BD faltantes (cero `CREATE INDEX` en las 3 migraciones salvo uno no relacionado), tuning del pool de conexiones (pg default max=10 sin ajustar a límites de Neon), logging estructurado, ADR faltante para el sistema de revancha 1v1 y para `walkover` en TECH-DESIGN §3.1, corregir TECH-DESIGN §7 (dice validación de sessionToken por evento, pero es solo en el handshake — funcionalmente correcto, doc desactualizada) | #12 (orden recomendado) | Ver obs #280, #281 | ✅ Implementado, verificado y archivado; mergeado a `master` (tracker PR #13, `39fec73`, 2026-08-31). 7 entregables: sanitización de nickname server-side, migración 0005 (7 índices), tuning de pool + graceful shutdown, logging `pino`, ADR-0010/0011/0012, correcciones TECH-DESIGN, aislamiento dev/prod DB. |
| 19 | Fase 7: Re-skin de pantallas contra prototipos + topbar canónico + modal de reglas | **Opción A del análisis 2026-08-30**: portar el bloque `<style>` propio de cada `Prototipos/N.*.html` a `frontend/src/assets/pokeduels-design-system.css` (namespaces `.lobby-*`, `.draft-*`, `.wait-*`, `.duel-*`, `.switch-*`, `.rank-*`) y re-aplicar los `className` en los wrappers React que hoy tienen `className=""` o ninguno, siguiendo el DOM del proto. Login queda como está (~95% fiel). Orden peor-primero: Lobby → Selección de equipo → Sala de espera → Tablero de duelo → Ranking → Cambio de pokémon. Riesgo bajo salvo la animación de ataque (ver abajo): el resto es CSS aditivo + atributos `className`, cero lógica/estado/data-flow; los tests son por texto/rol. **Preservar y re-estilar los agregados propios** al vocabulario `.pd-*`: bots (`AGREGAR BOT` / quitar bot), toggle `LISTO` (sacar el verde `#22c55e` fuera de paleta → `var(--pd-yellow)`), `CAMBIAR EQUIPO`, `CAMBIAR APODO`, buscador + filtro de tipo del catálogo, lead selection / esperar-o-ver-ya / banners de duelo / modal de rendirse, modo forzado de cambio + `CONFIRMAR CAMBIO`, banners de error + skeletons. **Topbar canónico** (todas menos Login): un solo `ScreenTopbar` con el usuario (nickname) arriba-izquierda, el logo Poke-duels centrado arriba (absolute), y un slot de acción por pantalla a la derecha (SALIR / RENDIRSE / etc.). **Lobby — 2 controles nuevos**: a la derecha del usuario, botón "Reglas del juego"; a la derecha del todo, botón "Volver al inicio" → navega a `/` (Login). **Modal de reglas**: "Reglas del juego" abre un popup con el mismo estilo (`components/Modal` + `.pd-card` + `.pd-modal-overlay`, focus trap ya existente) con el texto corto de reglas (ver sección "Reglas del juego (modal, item #19)" al pie — aprobado por el usuario 2026-08-30). **Animación de ataque (Tablero de duelo)**: al resolverse un turno, el sprite del Pokémon atacante avanza rápido al centro y vuelve (~1 s ida / ~1 s vuelta) y el sprite del que recibe vibra ~1 s; aplica a ambos lados (mi Pokémon y el del rival). Requiere que el re-skin del Tablero reponga los sprites en la arena (`.sprite-wrap` / `.sprite-wrap--rival` del proto — hoy `DuelBoardScreen` no los renderiza). Keyframes CSS + disparo de clases desde React keyeado al `turnNumber` / snapshot de `duel:turn_resolved`. Motivo: hoy no se nota quién ataca y quién recibe. | #15, #16 (van antes; ambos mergeados a `master`) | Análisis prototipo↔React del 2026-08-30 (screenshots + tabla de gaps por pantalla, ~25–70 % de fidelidad salvo Login). Reglas de juego canónicas en `spec-juego-tipo-pokemon.md`. (Resuelto 2026-08-31: el valor correcto es 10 s por decisión del usuario; `TURN_TIMEOUT_SECONDS` pasó de 8 a 10 en `frontend/src/lib/duelBoard.ts`, alineado con `backend/ws/turnTimers.js` `DEFAULT_TURN_TIMEOUT_MS = 10_000` y RF-6.1.) Decisión de diseño para la animación de ataque (a resolver en el spec): (a) sin tocar backend — el cliente infiere la secuencia desde `duel:turn_resolved` (v1 no tiene fallos ni críticos, todo ataque conecta; si un Pokémon queda `fainted` en el snapshot se omite su golpe); (b) con backend — enriquecer `duel:turn_resolved` con el orden real `[{attackerId, defenderId, moveIndex, damage}]`. La regla blanket de `prefers-reduced-motion` (Fase 5) ya desactiva estas animaciones sin trabajo extra. | ✅ Implementado y archivado (`sdd/fase-7-reskin-prototipos`, 2026-08-31) — 30/30 tasks, 10-PR feature-branch-chain, suites green (frontend 349/349, backend 453/453, `tsc -b` 0, `oxlint` 0). Todos los PRs aplicados y verificados. Cadena de branches: `fase-7-reskin-prototipos-pr1-topbar` @ `2a439ef` → … → `fase-7-reskin-prototipos-pr10-swap` @ `f5c4797` (tip). Carry-forward: manual screenshot comparison (6 screens vs Prototipos), merge order Phase 4 → Phase 5 → Fase 7 to `master`. |

## Cómo usar este backlog

Cada ítem es una spec independiente. Al implementarlo, arrancá un ciclo de Spec-Driven
Development (`sdd-new` o el flujo equivalente de tu harness) usando este ítem como el
"change" — no el proyecto completo. Si la columna "Contexto extra requerido" tiene algo,
compartilo como contexto al generar la spec de ese ítem.

## Reglas del juego (modal, item #19)

> Contenido del popup "Reglas del juego" del Lobby (item #19). Aprobado por el usuario 2026-08-30.
> Fuente: `spec-juego-tipo-pokemon.md`.

### ⚡ DUELOS POKÉMON

Sin niveles, objetos ni golpes críticos. Gana quien mejor maneje la ventaja de tipos y anticipe al rival.

**Armá tu Escuadrón**
- **Inicial único:** Pikachu, Bulbasaur, Squirtle o Charmander (el primero que lo confirma en la sala se lo queda).
- **Plantel:** Sumá 5 Pokémon del catálogo para completar tu 6 ideal.

**Reglas de Batalla**
- **Salud:** todos inician con 100 HP.
- **Ataques:** 4 movimientos fijos (25, 20 y 15 de daño con 4 PP cada uno; 10 de daño con uso ilimitado).
- **Tipos:** Supereficaz (x2), Poco eficaz (x0.5, mín. 1) y Neutral (x1). Sin inmunidades.
- **Combate a ciegas:** elección simultánea y prioridad al azar. Si nokeás al rival, cancelás su ataque.
- **Sin pausas:** cambiar de Pokémon o entrar tras un K.O. te permite atacar en esa misma ronda.
- **Tiempo límite:** si te quedás sin tiempo, se usa automáticamente el ataque de 10 de daño.
- **Desconexión:** si el rival se va, ganás el duelo al instante.

**Formato de Juego**
- **Salas de 2:** duelo directo. El ganador se corona Campeón.
- **Salas de 4:** torneo de 2 semifinales. Ganadores definen 1.º/2.º puesto y perdedores 3.º/4.º. HP y PP se restauran al 100 % entre partidas.
